<%= form_for [commentable, comment] do |f| %>
  <div class="fields">
    <%= f.text_area :body, rows: 2, cols: 80 %>
  </div>
  <div class="actions">
    <%= f.submit "Add Comment" %>
  </div>
<% end %>
